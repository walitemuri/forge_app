# Public DNS name, without scheme or trailing slash.
FORGE_DOMAIN=forge.example.com
# Set a long random value before the first database start.
FORGE_DB_PASSWORD=replace-with-a-long-random-password
# Set false to pause new launches while keeping history and playback available.
FORGE_DEMO_LAUNCH_ENABLED=true
